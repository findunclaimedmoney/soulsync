// Simplified app-params — no Base44 SDK dependency
export const appParams = {
  appId: 'glimr',
  token: null,
  functionsVersion: null,
  appBaseUrl: window.location.origin,
  fromUrl: window.location.href,
};
